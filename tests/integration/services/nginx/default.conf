server {
    listen 80;
    server_name localhost;

    location /api/status {
        default_type application/json;
        return 200 '{"status":"ok","server":"nginx","os":"SyntropicOS"}';
    }

    location / {
        default_type text/plain;
        return 200 'Nginx 3rd-Party HTTP Server Ready';
    }
}
