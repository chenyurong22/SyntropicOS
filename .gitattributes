# Auto detect text files and perform LF normalization
* text=auto

# Enforce strict LF line endings for shell scripts and git hooks on Windows
*.sh text eol=lf
.githooks/* text eol=lf
Makefile text eol=lf
